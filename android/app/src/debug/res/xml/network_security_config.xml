<?xml version="1.0" encoding="utf-8"?>
<!-- Debug only: allow HTTP to local dev server (emulator 10.0.2.2, LAN IP). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
