# Local development (server/server-local.js)
PORT=8787
WS_HISTORY_LIMIT=80
# MESSAGE_TTL_SEC=7200
# MAX_MESSAGES_PER_ROOM=100
# ENABLE_LEGACY_HTTP=1
# TRANSPORT_LOG=1
# RELAY_LOG=1

# Cloudflare Workers (set via wrangler secret / dashboard, not in repo)
# ENVIRONMENT=production
